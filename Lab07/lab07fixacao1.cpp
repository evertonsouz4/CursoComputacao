#include <iostream>
using namespace std;

char convert_ascii(char num){
	return num;
}

int main(){
	 cout << convert_ascii(80);
	 cout << convert_ascii(14);
	 cout << convert_ascii(111);
	 cout << convert_ascii(103);
	 cout << convert_ascii(67);
	 cout << convert_ascii(111);
	 cout << convert_ascii(109);
	 cout << convert_ascii(112) << "\n";

	return 0;
}
